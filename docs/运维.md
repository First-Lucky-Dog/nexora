
1. 重新构建后端，保留本地数据
docker compose -f docker-compose.dev.yml up -d --build new-api

2. 查看构建后的容器状态
docker compose -f docker-compose.dev.yml ps

3. 查看后端日志
docker compose -f docker-compose.dev.yml logs -f new-api

4. 测试后端健康状态
curl http://localhost:3000/api/status

5. 代码没有生效，再用无缓存重建：

docker compose -f docker-compose.dev.yml build --no-cache new-api

docker compose -f docker-compose.dev.yml up -d new-api

curl.exe "http://localhost:3000/v1/images/edits" `   -H "Authorization: Bearer sk-InAjCyKp0EUlwsKZHzrGHtkVnLBYOA0TsjfKCoXsYcbV32Xd" `   -F "model=nanobanana" `   -F "prompt=把这张图改成白色背景、高质量商品图风格" `   -F "image=@C:\Users\lucky\Pictures\Screenshots\屏幕截图 2026-05-20 151854.png" `    -F "n=1" `   -F "size=1024x1024" `   -F "response_format=b64_json"