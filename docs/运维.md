
1. 重新构建后端，保留本地数据
docker compose -f docker-compose.dev.yml up -d --build new-api

2. 查看构建后的容器状态
docker compose -f docker-compose.dev.yml ps

3. 查看后端日志
docker compose -f docker-compose.dev.yml logs -f new-api

4. 测试后端健康状态
curl http://localhost:3000/api/status

5. 代码没有生效，再用无缓存重建：

docker compose -f docker-compose.dev.yml build --no-cache new-api

docker compose -f docker-compose.dev.yml up -d new-api

火山云：
重新拉取
cd /data/source/nexora

echo "===== 设置当前仓库 Git 使用 HTTP/1.1 ====="
git config http.version HTTP/1.1

echo
echo "===== 确认配置 ====="
git config --get http.version

echo
echo "===== 重新拉取 ====="
git pull --ff-only

echo
echo "===== 更新后提交 ====="
git log -1 --oneline

echo
echo "===== 工作区状态 ====="
git status --short